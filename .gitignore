# Editor and OS
.DS_Store
.vscode/
.idea/

# Local build or validation output
node_modules/
.venv/
__pycache__/
*.tmp
.cache/

# Environment
.env
.env.*
!.env.example

# Build scratch. Charts are rendered into the version folder while the PDF is
# assembled and removed afterwards; a failed build leaves them behind.
**/_charts/
**/_build.md
**/_test.md
**/.zenodo.json
